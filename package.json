{
  "name": "9bot",
  "version": "1.0.0",
  "description": "discord bot for :9:",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean && npx tsc && npx tsc-alias",
    "start": "npm run build && node dist",
    "watch": "npm run clean && npx tsc -w && npx tsc-alias -w",
    "deploy": "npm run build && node dist/deploy-commands.js",
    "test": "echo 🤫🧏",
    "clean": "npx rimraf dist"
  },
  "author": "9",
  "license": "ISC",
  "dependencies": {
    "@seald-io/nedb": "^4.0.4",
    "cron": "^3.1.6",
    "discord.js": "^14.14.1",
    "dotenv": "^16.4.5",
    "googleapis": "^133.0.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@types/cron": "^2.4.0",
    "@types/node": "^18.13.0",
    "eslint": "^8.57.0",
    "prettier": "^2.8.4",
    "rimraf": "^5.0.5",
    "tsc-alias": "^1.8.8",
    "typescript": "^5.4.2"
  }
}
